package com.nya.mitzi.validator;

public class CategoryValidator  {

    //@Autowired
    //private CategoryRepo categoryRepo;

   /* public boolean isUnique(Category category) throws CategoryException {
        if(this.categoryRepo.existsById(Math.toIntExact(category.getCategoryId()))){
            throw new CategoryException(ErrorMessage.ID_ALREADY_FOUND);
        }
        if(this.categoryRepo.existsByName(category.getName())){
            throw new CategoryException(ErrorMessage.CATEGORY_NAME_EXIST);
        }
        return true;
    }*/

}
